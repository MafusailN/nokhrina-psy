# Сайт-визитка · психолог Юлия Нохрина

Статичный одностраничник: HTML + CSS + чистый JS, без сборки и зависимостей.
Открывается двойным кликом по `index.html`, заливается на любой хостинг (GitHub Pages,
Netlify, Vercel, обычный хостинг с FTP) простым копированием папки.

```
index.html
assets/
  css/styles.css
  js/main.js
  img/            ← сюда фотографии
```

## Что нужно заменить перед публикацией

| Где | Что |
|---|---|
| `assets/img/portrait.jpg` | ✅ взято с Avito (384×480 — стоит заменить оригиналом, см. ниже) |
| `assets/img/about.jpg` | квадратное фото для блока «Обо мне» — см. `tools/crop-avatar.ps1` |
| `assets/js/main.js`, `CONFIG` | ❗ адрес формы Formspree (`ENDPOINT`) — пока заглушка |
| контакты | ✅ Telegram `@yuliyanokhrina`, почта `Nokhrina_y@mail.ru` |

Пока фото не положены, на их месте показывается аккуратная заглушка — сайт не ломается.

### Фото

`portrait.jpg` скачан из объявления на Avito и обрезан под 4:5 (водяной знак срезан).
Но Avito отдаёт превью 640×480 — для первого экрана это впритык, на телефонах с
Retina-экраном будет чуть мягко. Лучше попросить у Юлии оригинал этого кадра и
положить его на то же место — размером хотя бы 1200×1500.

`about.jpg` готовится скриптом `tools/crop-avatar.ps1`: сохраните фото в
`assets/img/about-raw.jpg`, запустите скрипт — он обрежет кадр под 4:5 (900×1125)
и срежет нижнюю полосу с реакциями и кружками сторис. Если рамка съедет, в начале
скрипта есть коэффициенты `CUT_BOTTOM / CUT_TOP / SHIFT_X`.

## Форма записи

Заявка уходит письмом на `Nokhrina_y@mail.ru` через [FormSubmit](https://formsubmit.co) —
сервису не нужны ни регистрация, ни свой сервер. Посетитель ничего не открывает:
нажал кнопку и увидел «Спасибо».

### Разовая активация адреса

Первую заявку сервис придержит и пришлёт на почту письмо
**«Confirm your email»** со ссылкой активации. По ней нужно перейти один раз —
после этого письма начнут приходить сразу. Если письма нет, проверьте «Спам».

### Скрыть адрес от спам-ботов

Сейчас почта указана в `assets/js/main.js` открытым текстом, а файл публичный —
её могут собрать спам-боты. После активации FormSubmit показывает «скрытый» адрес
вида `https://formsubmit.co/ajax/a1b2c3...`. Его стоит подставить в `CONFIG.ENDPOINT`
вместо почты. Найти его можно в том же письме активации.

### Если что-то пойдёт не так

При ошибке сети форма не молчит: показывает сообщение со ссылками на Telegram
и почту, чтобы человек всё равно смог написать.

Скрытое поле-ловушка `_gotcha` отсекает ботов, заполняющих все поля подряд.

## Дублирование в Telegram (когда понадобится)

Сейчас записи ведутся вручную. Когда захочется получать заявки в Telegram:

1. Создать бота через [@BotFather](https://t.me/BotFather), получить токен.
2. Написать боту любое сообщение и узнать свой `chat_id`
   через `https://api.telegram.org/bot<ТОКЕН>/getUpdates`.
3. Дальше два пути:
   - **Без кода** — подключить Formspree к Make/Zapier, они перешлют заявку в Telegram.
   - **С кодом** — небольшая функция на Netlify/Vercel, которая принимает форму и
     дёргает `sendMessage`. Токен бота нельзя класть в `main.js` — он виден всем.

## Публикация

Сайт живёт на GitHub Pages: https://mafusailn.github.io/nokhrina-psy/

Обновление — обычный push, сборка занимает минуту-две:

```powershell
git add -A
git commit -m "что изменено"
git push
```

## Юридическое

Страница `privacy.html` — политика обработки персональных данных по 152-ФЗ,
ссылки на неё стоят в чекбоксе согласия и в подвале.

Что стоит уточнить у Юлии и дописать: её налоговый статус (самозанятая или ИП),
полное ФИО и, если есть, ИНН. Для физлица без статуса текущей редакции достаточно,
но при регистрации самозанятости реквизиты лучше указать.
